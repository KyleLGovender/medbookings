/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["wfhq93qoozwnz2zu.public.blob.vercel-storage.com"],
  },
  // ... other config options
};

module.exports = nextConfig;
