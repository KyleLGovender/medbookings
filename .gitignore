# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage
/test-results
/playwright-report
e2e/.auth/user.json

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files (CRITICAL - NEVER commit secrets)
# SECURITY: These files contain API keys, database credentials, and authentication secrets
# Production secrets: Managed ONLY in Vercel Dashboard
# Local development: Use .env.local (gitignored)
# Template: .env.example (tracked, no secrets)
.env
.env.local
.env*.local
.env.production
.env.production.local


# typescript
*.tsbuildinfo
next-env.d.ts

docker_data

# logs
/logs/
logs/

# Personal workflow system (local only - not shared with team)
# SECURITY: These files may contain credentials - NEVER commit them
package.json.local
.workflow-enabled
run-local.js

# Personal workflow working directories (work in progress)
workflow/prps/
workflow/patterns/staging/
workflow/technical-plans/

# Personal Claude settings (may contain API keys and credentials)
# SECURITY: Contains permissions and potentially sensitive configuration
.claude/settings.personal.json

# Keep these workflow directories but ignore their contents initially
workflow/prps/**/*
!workflow/prps/**/.gitkeep
workflow/patterns/staging/**/*
!workflow/patterns/staging/**/.gitkeep
workflow/technical-plans/**/*
!workflow/technical-plans/**/.gitkeep
.vercel
