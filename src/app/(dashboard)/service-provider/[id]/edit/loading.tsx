import CalendarLoader from '@/components/calendar-loader';

export default function ServiceProviderEditLoading() {
  return <CalendarLoader message="Loading Editor" submessage="Preparing provider editor..." />;
}
